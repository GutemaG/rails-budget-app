<nav class="navbar navbar-expand-lg navbar-main bg-main">
  <div class="container-fluid d-flex justify-content-between">
    <button class="navbar-toggler border-0" type="button">
      <% if controller_name == 'groups' && action_name == 'index'%>
        <span class="text-main"><i class="fa-solid fa-bars"></i> </span>
      <%else%>
        <%= link_to "javascript:history.back()" do %>
          <i class="fa-solid fa-arrow-left text-white"></i>
        <%end%>
      <%end%>
    </button>
    <div class="navbar-brand mx-auto text-white"><%= title %></div>
    <div class="d-flex gap-3 align-items-center">
      <%= button_to destroy_user_session_path, method: :delete, data: { turbo: false }, class: "btn" do %>
        <i class="fa fa-sign-out"></i>
      <%end%>
      <span>
        <i class="fa fa-search"></i>
      </span>
    </div>
  </div>
</nav>
